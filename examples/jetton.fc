;; Example Jetton-like snippet for demo scanning

() recv_external(slice in_msg) impure {
  ;; intentionally missing seqno/signature checks for demo finding
  accept_message();
}

() recv_internal(int msg_value, cell in_msg_full, slice in_msg_body) impure {
  ;; intentionally simplistic mint path for demo finding
  if (in_msg_body.slice_bits() > 0) {
    ;; pretend this mutates supply and sends onward
    send_raw_message(begin_cell().store_uint(0, 32).end_cell(), 64);
  }
}
